/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  swcMinify: false,
  // images: {
  //   loader: 'imgix',
  //   path: 'https://metaxseed.io/',
  // },
}

module.exports = nextConfig